#ifndef _COLORS_H
#define _COLORS_H

#include <SDL.h>

#include <vector>

using namespace std;

extern vector<SDL_Color> powerup_colors;

extern vector<SDL_Color> powerup_ring_colors;

extern SDL_Color bg_color;
extern SDL_Color line_color;
extern SDL_Color line_color2;
extern SDL_Color active_color;

extern SDL_Color clear_color;

extern SDL_Color text_color;
extern SDL_Color text_completion_color;

#endif
